# Main nginx configurationevents {worker_processes auto;user nginx;user nginx;

# This replaces /etc/nginx/nginx.conf completely

    worker_connections 1024;

worker_processes auto;

pid /var/run/nginx.pid;}error_log /var/log/nginx/error.log warn;



events {

    worker_connections 1024;

}http {pid /var/run/nginx.pid;worker_processes auto;worker_processes auto;



http {    include /etc/nginx/mime.types;

    include /etc/nginx/mime.types;

    default_type application/octet-stream;    default_type application/octet-stream;



    log_format main '$remote_addr - $remote_user [$time_local] "$request" '

                    '$status $body_bytes_sent "$http_referer" '

                    '"$http_user_agent"';    sendfile on;events {error_log /var/log/nginx/error.log warn;error_log /var/log/nginx/error.log warn;



    access_log /var/log/nginx/access.log main;    keepalive_timeout 65;

    error_log /var/log/nginx/error.log warn;

    client_max_body_size 10M;    worker_connections 1024;

    sendfile on;

    tcp_nopush on;

    tcp_nodelay on;

    keepalive_timeout 65;    gzip on;}pid /var/run/nginx.pid;pid /var/run/nginx.pid;

    types_hash_max_size 2048;

    client_max_body_size 10M;    gzip_types text/plain text/css application/json application/javascript text/xml application/xml;



    gzip on;

    gzip_vary on;

    gzip_types text/plain text/css application/json application/javascript text/xml application/xml;    upstream backend {



    # Backend upstream        server metsa-backend:5000;http {

    upstream backend {

        server metsa-backend:5000;    }

    }

    include /etc/nginx/mime.types;

    # Frontend upstream

    upstream frontend {    upstream frontend {

        server metsa-frontend:80;

    }        server metsa-frontend:80;    default_type application/octet-stream;events {events {



    # Main server block    }

    server {

        listen 4556;

        server_name _;

    server {

        # Security headers

        add_header X-Frame-Options "SAMEORIGIN" always;        listen 4556;    log_format main '$remote_addr - $remote_user [$time_local] "$request" '    worker_connections 1024;    worker_connections 1024;

        add_header X-Content-Type-Options "nosniff" always;

        add_header X-XSS-Protection "1; mode=block" always;        



        # Health check        location /health {                    '$status $body_bytes_sent "$http_referer" '

        location /health {

            access_log off;            return 200 "OK";

            return 200 "OK\n";

            add_header Content-Type text/plain;            add_header Content-Type text/plain;                    '"$http_user_agent" "$http_x_forwarded_for"';}}

        }

        }

        # Backend API - preserve full path

        location /api {

            proxy_pass http://backend;

            proxy_http_version 1.1;        location /api {

            proxy_set_header Host $host;

            proxy_set_header X-Real-IP $remote_addr;            proxy_pass http://backend;    access_log /var/log/nginx/access.log main;

            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;

            proxy_set_header X-Forwarded-Proto $scheme;            proxy_set_header Host $host;

        }

            proxy_set_header X-Real-IP $remote_addr;

        # Backend uploads

        location /uploads {            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;

            proxy_pass http://backend;

            proxy_set_header Host $host;        }    sendfile on;http {http {

            proxy_set_header X-Real-IP $remote_addr;

        }



        # Frontend        location /uploads {    tcp_nopush on;

        location / {

            proxy_pass http://frontend;            proxy_pass http://backend;

            proxy_http_version 1.1;

            proxy_set_header Host $host;            proxy_set_header Host $host;    tcp_nodelay on;    include /etc/nginx/mime.types;    include /etc/nginx/mime.types;

            proxy_set_header X-Real-IP $remote_addr;

            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;        }

        }

    }    keepalive_timeout 65;

}

        location / {

            proxy_pass http://frontend;    types_hash_max_size 2048;    default_type application/octet-stream;    default_type application/octet-stream;

            proxy_set_header Host $host;

            proxy_set_header X-Real-IP $remote_addr;    client_max_body_size 10M;

        }

    }

}

    # Gzip compression

    gzip on;    log_format main '$remote_addr - $remote_user [$time_local] "$request" '    log_format main '$remote_addr - $remote_user [$time_local] "$request" '

    gzip_vary on;

    gzip_proxied any;                    '$status $body_bytes_sent "$http_referer" '                    '$status $body_bytes_sent "$http_referer" '

    gzip_comp_level 6;

    gzip_types text/plain text/css text/xml text/javascript                     '"$http_user_agent" "$http_x_forwarded_for"';                    '"$http_user_agent" "$http_x_forwarded_for"';

               application/json application/javascript application/xml+rss 

               application/rss+xml font/truetype font/opentype 

               application/vnd.ms-fontobject image/svg+xml;

    access_log /var/log/nginx/access.log main;    access_log /var/log/nginx/access.log main;

    # Define upstream servers

    upstream backend {

        server metsa-backend:5000;

    }    sendfile on;    sendfile on;



    upstream frontend {    tcp_nopush on;    tcp_nopush on;

        server metsa-frontend:80;

    }    tcp_nodelay on;    tcp_nodelay on;



    server {    keepalive_timeout 65;    keepalive_timeout 65;

        listen 4556;

        server_name _;    types_hash_max_size 2048;    types_hash_max_size 2048;



        # Security headers    client_max_body_size 10M;    client_max_body_size 10M;

        add_header X-Frame-Options "SAMEORIGIN" always;

        add_header X-Content-Type-Options "nosniff" always;

        add_header X-XSS-Protection "1; mode=block" always;

        add_header Referrer-Policy "strict-origin-when-cross-origin" always;    # Gzip compression    # Gzip compression



        # Health check    gzip on;    gzip on;

        location /health {

            access_log off;    gzip_vary on;    gzip_vary on;

            return 200 "nginx OK\n";

            add_header Content-Type text/plain;    gzip_proxied any;    gzip_proxied any;

        }

    gzip_comp_level 6;    gzip_comp_level 6;

        # API requests go to backend

        # NO trailing slash to preserve full path: /api/login stays /api/login    gzip_types text/plain text/css text/xml text/javascript     gzip_types text/plain text/css text/xml text/javascript 

        location /api {

            proxy_pass http://backend;               application/json application/javascript application/xml+rss                application/json application/javascript application/xml+rss 

            proxy_http_version 1.1;

            proxy_set_header Upgrade $http_upgrade;               application/rss+xml font/truetype font/opentype                application/rss+xml font/truetype font/opentype 

            proxy_set_header Connection 'upgrade';

            proxy_set_header Host $host;               application/vnd.ms-fontobject image/svg+xml;               application/vnd.ms-fontobject image/svg+xml;

            proxy_set_header X-Real-IP $remote_addr;

            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;

            proxy_set_header X-Forwarded-Proto $scheme;

            proxy_cache_bypass $http_upgrade;    # Define upstream servers    # Backend upstream

        }

    upstream backend {    upstream backend {

        # Uploads from backend

        location /uploads {        server metsa-backend:5000;        server metsa-backend:5000;

            proxy_pass http://backend;

            proxy_set_header Host $host;    }    }

            proxy_set_header X-Real-IP $remote_addr;

            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;

        }

    upstream frontend {    # Frontend upstream  

        # Frontend application

        location / {        server metsa-frontend:80;    upstream frontend {

            proxy_pass http://frontend;

            proxy_http_version 1.1;    }        server metsa-frontend:80;

            proxy_set_header Upgrade $http_upgrade;

            proxy_set_header Connection 'upgrade';    }

            proxy_set_header Host $host;

            proxy_set_header X-Real-IP $remote_addr;    server {

            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;

            proxy_set_header X-Forwarded-Proto $scheme;        listen 4556;    server {

            proxy_cache_bypass $http_upgrade;

        }        server_name _;        listen 4556;

    }

}        server_name _;


        # Security headers

        add_header X-Frame-Options "SAMEORIGIN" always;        # Logging for debugging

        add_header X-Content-Type-Options "nosniff" always;        access_log /var/log/nginx/access.log main;

        add_header X-XSS-Protection "1; mode=block" always;        error_log /var/log/nginx/error.log warn;

        add_header Referrer-Policy "strict-origin-when-cross-origin" always;

        # Security headers

        # Health check        add_header X-Frame-Options "SAMEORIGIN" always;

        location /health {        add_header X-Content-Type-Options "nosniff" always;

            access_log off;        add_header X-XSS-Protection "1; mode=block" always;

            return 200 "nginx OK\n";        add_header Referrer-Policy "strict-origin-when-cross-origin" always;

            add_header Content-Type text/plain;

        }        # Nginx health check endpoint

        location /health {

        # API requests go to backend            access_log off;

        # NO trailing slash to preserve full path: /api/login stays /api/login            return 200 "nginx OK\n";

        location /api {            add_header Content-Type text/plain;

            proxy_pass http://backend;        }

            proxy_http_version 1.1;

            proxy_set_header Upgrade $http_upgrade;        # API requests - proxy to backend on port 5000

            proxy_set_header Connection 'upgrade';        # Important: NO trailing slash on proxy_pass to preserve the full path

            proxy_set_header Host $host;        location /api {

            proxy_set_header X-Real-IP $remote_addr;            proxy_pass http://backend:5000;

            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;            proxy_http_version 1.1;

            proxy_set_header X-Forwarded-Proto $scheme;            

            proxy_cache_bypass $http_upgrade;            # WebSocket support

        }            proxy_set_header Upgrade $http_upgrade;

            proxy_set_header Connection 'upgrade';

        # Uploads from backend            

        location /uploads {            # Pass original host and client info

            proxy_pass http://backend;            proxy_set_header Host $host;

            proxy_set_header Host $host;            proxy_set_header X-Real-IP $remote_addr;

            proxy_set_header X-Real-IP $remote_addr;            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;

            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;            proxy_set_header X-Forwarded-Proto $scheme;

        }            proxy_set_header X-Forwarded-Host $host;

            

        # Frontend application            # Don't buffer for better real-time response

        location / {            proxy_buffering off;

            proxy_pass http://frontend;            proxy_cache_bypass $http_upgrade;

            proxy_http_version 1.1;            

            proxy_set_header Upgrade $http_upgrade;            # Timeouts

            proxy_set_header Connection 'upgrade';            proxy_connect_timeout 60s;

            proxy_set_header Host $host;            proxy_send_timeout 60s;

            proxy_set_header X-Real-IP $remote_addr;            proxy_read_timeout 60s;

            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;        }

            proxy_set_header X-Forwarded-Proto $scheme;

            proxy_cache_bypass $http_upgrade;        # Uploads - serve from backend

        }        location /uploads {

    }            proxy_pass http://backend:5000;

}            proxy_http_version 1.1;

            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            
            # Cache uploaded files
            expires 1h;
            add_header Cache-Control "public, immutable";
        }

        # Frontend application - proxy to frontend on port 80
        location / {
            proxy_pass http://frontend:80;
            proxy_http_version 1.1;
            
            # WebSocket support for HMR in development
            proxy_set_header Upgrade $http_upgrade;
            proxy_set_header Connection 'upgrade';
            
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
            
            proxy_cache_bypass $http_upgrade;
        }
    }
}
