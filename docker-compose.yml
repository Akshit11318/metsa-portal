services:
  # Backend API Service
  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: metsa-backend
    restart: unless-stopped
    environment:
      - NODE_ENV=production
      - PORT=5000
      - DATABASE_URL=file:./dev.db
      - CORS_ORIGIN=https://metsa.kryptolo121.xyz
      - CSRF_ENABLED=true
      - JWT_SECRET=metsa-portal-2025-secret-key-change-in-production
      - DEFAULT_YEAR=2025
    volumes:
      - backend-db:/app/prisma
      - ./backend/uploads:/app/uploads
      - backend-data:/app/data
    networks:
      - metsa-network
    healthcheck:
      test: ["CMD", "wget", "--quiet", "--tries=1", "--spider", "http://localhost:5000/"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s

  # Frontend Service
  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    container_name: metsa-frontend
    restart: unless-stopped
    networks:
      - metsa-network
    depends_on:
      - backend
    healthcheck:
      test: ["CMD", "wget", "--quiet", "--tries=1", "--spider", "http://localhost:80/"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s

  # Nginx Reverse Proxy
  nginx:
    image: nginx:alpine
    container_name: metsa-nginx
    restart: unless-stopped
    ports:
      - "4556:4556"
    volumes:
      - ./nginx/nginx.conf:/etc/nginx/nginx.conf:ro
    networks:
      - metsa-network
    depends_on:
      - backend
      - frontend
    healthcheck:
      test: ["CMD", "wget", "--quiet", "--tries=1", "--spider", "http://localhost:4556/health"]
      interval: 30s
      timeout: 10s
      retries: 3

networks:
  metsa-network:
    driver: bridge

volumes:
  backend-db:
    driver: local
  backend-data:
    driver: local
